void update(int *ptr){
    
   
    *ptr=*ptr+1; // value is not udated 
}

int main() {
    
    int value=5;
    
    int *ptr=&value;
   
    cout<<" Before "<<*ptr<<endl; // 5
    update(ptr);
    cout<<" After"<<*ptr<<endl;  //  6

    return 0;
}
